[package]
name = "pop3-server"
version = "0.1.0"
edition = "2024"

[dependencies]
auth = { path = "../auth" }
maildir = { path = "../maildir" }
sled = "0.34.7"
tokio = { version = "1.45.1", features = ["full"] }
